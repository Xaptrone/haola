---
description: fxgen visual system — dark canvas, purple accent, Sora, video-first. Apply on all UI work.
alwaysApply: true
---
# fxgen UI

Before any visual change, read `.cursor/skills/haola-uiux/SKILL.md`.

Tokens win: canvas `#12110F`, surface `#1E1C18`, accent `#6F43F6`, type Sora. Use CSS variables / Tailwind tokens — no hex in JSX.

Landing = type + one 9:16 reel + one purple CTA + fxgen logo. Creator desktop = blank canvas studio. Business login = next actions, not charts. One accent. No Inter, no feature-card grids in the hero, no sidebar on work routes.
